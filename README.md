# search_launcher
Search_launcher is a command-line Python script that searches the Python Package Index (PyPI) for user-supplied keywords and automatically opens the top matching package results in separate browser tabs.
